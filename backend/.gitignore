# Dependencies
node_modules/
jspm_packages/

# .env files (never commit secrets)
.env
.env.local
.env.*.local

# .env.production.example IS committed (it has no real secrets, just a template)

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Upload files (user-generated content)
uploads/

# Build/Dist
dist/
build/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Puppeteer / Chromium cache
.local-chromium/
.cache/

# Package lock (optional — keep for reproducible installs)
# package-lock.json
